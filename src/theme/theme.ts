const colors = {
  accent: {
    light: "#60a5fa",
    DEFAULT: "#3b82f6",
    dark: "#2563eb",
  },
};

const spacing = {
  sx: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
};

const radii = {
  sm: 4, // tags and badges ([ /min])
  md: 8, // buttons and inputs
  lg: 12, // card and modals
  full: 9999,
};

const fontSizes = {
  caption: 12, // [ /min]
  body: 15,
  button: 16,
  subhead: 16,
  title: 20, // section titles
  headline: 24, // main titles
};

const elevations = {
  none: 0,
  card: 2,
  fab: 6,
  modal: 8,
};

export const theme = {
  colors,
  spacing,
  radii,
  fontSizes,
  elevations,
};
